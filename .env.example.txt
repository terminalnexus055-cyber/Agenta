# CODEX Agent — Environment Variables
# Add these in Vercel Dashboard → Settings → Environment Variables

# ── REQUIRED ──
GROQ_API_KEY=your_groq_key_here

# ── RECOMMENDED ──
GEMINI_API_KEY=your_gemini_key_here

# ── OPTIONAL (higher GitHub API rate limits) ──
# Without this: 60 requests/hour
# With this: 5000 requests/hour
GITHUB_TOKEN=your_github_pat_here

# ── WHERE TO GET KEYS ──
# Groq:   https://console.groq.com  (free, generous limits)
# Gemini: https://aistudio.google.com/app/apikey  (free tier)
# GitHub: https://github.com/settings/tokens → New token → repo:read scope only
